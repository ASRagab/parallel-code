# Tasks — Add Focus Mode Shortcut

- [x] Add a shared store helper for toggling focus mode on a specific task or
      the active task.
- [x] Add a configurable app-layer keybinding and wire it to the app shortcut
      action registry.
- [x] Cover the new default binding in the keybinding defaults test suite.
- [ ] Validate with `npm run check`, `npm test`, and `openspec validate --all --strict`.
